# 🏗️ ארכיטקטורת המערכת - SmarTrading Backend

## תרשים מערכת

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend / Base44                        │
│                   (React UI + AI Prompts)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP GET Request
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Express.js Server (Port 3000)                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      Middleware Layer                      │  │
│  │  • CORS (Cross-Origin)                                    │  │
│  │  • Rate Limiting (10 req/5min)                            │  │
│  │  • Request Logging                                         │  │
│  │  • Error Handling                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Routes: /api/ai-market-analyzer                 │
│                                                                   │
│  GET /btc             → Market snapshot                          │
│  GET /cache-stats     → Cache info                               │
│  POST /clear-cache    → Clear cache                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Cache Check   │
                    │  (30min TTL)   │
                    └────┬───────┬───┘
                         │       │
                    Hit  │       │  Miss
                         │       │
                         ▼       ▼
                    ┌────────┐  ┌──────────────────────────────┐
                    │ Return │  │  marketAnalyzerService.js    │
                    │ Cached │  │  (Main Orchestrator)         │
                    │  Data  │  │                              │
                    └────────┘  └──────┬───────────────────────┘
                                       │
                        ┌──────────────┴──────────────┐
                        ▼                             ▼
        ┌───────────────────────────┐  ┌──────────────────────────┐
        │   binanceService.js       │  │   bybitService.js        │
        │   (Retail Data)           │  │   (Whale/Institutional)  │
        │                           │  │                          │
        │  • BTCUSDT Futures        │  │  • BTCUSD Inverse        │
        │  • 24h Ticker             │  │  • 24h Ticker            │
        │  • Open Interest          │  │  • Open Interest         │
        │  • Funding Rate           │  │  • Funding Rate          │
        │  • Long/Short Ratio       │  │  • Long/Short Ratio      │
        │  • Volume                 │  │  • Volume                │
        │  • CVD Signal             │  │  • CVD Signal            │
        └──────────┬────────────────┘  └──────────┬───────────────┘
                   │                              │
                   │    Public APIs (FREE!)       │
                   │                              │
                   ▼                              ▼
        ┌──────────────────────┐      ┌──────────────────────┐
        │  Binance Futures API │      │  Bybit API v5        │
        │  fapi.binance.com    │      │  api.bybit.com       │
        └──────────────────────┘      └──────────────────────┘
                   │                              │
                   └──────────────┬───────────────┘
                                  │
                                  ▼
                          ┌───────────────┐
                          │ Data Merge &  │
                          │  Validation   │
                          └───────┬───────┘
                                  │
                                  ▼
                          ┌───────────────┐
                          │  Cache Store  │
                          │  (30 minutes) │
                          └───────┬───────┘
                                  │
                                  ▼
                          ┌───────────────┐
                          │ Return JSON   │
                          └───────────────┘
```

---

## 🔄 Cron Job - Auto Refresh

```
                ┌──────────────────────────┐
                │   Node-Cron Scheduler    │
                │   (Every 30 minutes)     │
                └────────┬─────────────────┘
                         │
                         │ Trigger
                         │
                         ▼
                ┌─────────────────────────┐
                │  Pre-fetch Market Data   │
                │  (Background Process)    │
                └────────┬────────────────┘
                         │
                         ▼
                ┌─────────────────────────┐
                │   Store in Cache        │
                │   (Fresh for 30min)     │
                └─────────────────────────┘
```

**תוצאה:** כשמשתמש מבקש נתונים, הם כבר מוכנים ב-cache!

---

## 📊 Data Flow למשתמש

```
User clicks "Analyze" button
         │
         ▼
┌────────────────────┐
│  Base44 HTTP GET   │
│  to Backend API    │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐       ┌─────────────┐
│  Express receives  │  Yes  │   Return    │
│  Check cache?      ├──────▶│   cached    │
└────────┬───────────┘       │   data      │
         │ No                └─────────────┘
         ▼
┌────────────────────┐
│ Fetch from APIs    │
│ • Binance          │
│ • Bybit            │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Merge & Validate   │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Store in cache     │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Return JSON to     │
│ Base44             │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Base44 AI Model    │
│ Analyzes data      │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Display results:   │
│ • Macro            │
│ • Micro            │
│ • Super-Micro      │
└────────────────────┘
```

---

## 🗂️ מבנה קבצים + תפקידים

```
smartrading-backend/
│
├── src/
│   ├── index.js                    # 🚀 Entry Point
│   │   ├── Express server setup
│   │   ├── Middleware config
│   │   ├── Routes registration
│   │   ├── Cron job initialization
│   │   └── Error handling
│   │
│   ├── routes/
│   │   └── marketAnalyzer.js      # 🛣️ API Endpoints
│   │       ├── GET /btc
│   │       ├── GET /cache-stats
│   │       └── POST /clear-cache
│   │
│   ├── services/
│   │   ├── binanceService.js      # 📊 Binance Integration
│   │   │   ├── get24hTicker()
│   │   │   ├── getOpenInterest()
│   │   │   ├── getFundingRate()
│   │   │   └── getLongShortRatio()
│   │   │
│   │   ├── bybitService.js        # 📈 Bybit Integration
│   │   │   ├── getTicker()
│   │   │   ├── getOpenInterest()
│   │   │   └── getFundingRate()
│   │   │
│   │   └── marketAnalyzerService.js  # 🧠 Main Logic
│   │       ├── getMarketSnapshot()
│   │       ├── getLiquidationsData()
│   │       ├── validateSnapshot()
│   │       └── Fallback handlers
│   │
│   └── utils/
│       ├── logger.js              # 📝 Winston Logger
│       │   ├── Console logging (dev)
│       │   ├── File logging (prod)
│       │   └── Error/Info levels
│       │
│       └── cache.js               # 💾 Cache Manager
│           ├── set(key, value)
│           ├── get(key)
│           ├── clear(key)
│           └── getStats()
│
├── logs/
│   ├── error.log                  # ❌ Errors only
│   └── combined.log               # 📜 All logs
│
├── .env                           # ⚙️ Configuration
├── package.json                   # 📦 Dependencies
└── Documentation/                 # 📚 Guides
    ├── QUICKSTART.md
    ├── README.md
    ├── DEPLOYMENT.md
    └── BASE44_INTEGRATION.md
```

---

## ⚡ Performance Characteristics

### Latency
- **Cache Hit:** < 10ms
- **Cache Miss:** 500-1000ms (depends on API response)
- **Cold Start:** ~2-3 seconds (initial cache population)

### Memory Usage
- **Base:** ~50MB (Node.js + Express)
- **With Cache:** ~70-100MB
- **Peak:** ~150MB (during heavy API calls)

### CPU Usage
- **Idle:** < 1%
- **API Calls:** 5-15% (brief spikes)
- **Cron Job:** 10-20% (every 30min, for ~2 seconds)

---

## 🔒 Security Layers

```
┌──────────────────────────────────────────┐
│         1. Rate Limiting Layer           │
│    Max 10 requests / 5 minutes / IP      │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│         2. CORS Protection               │
│    Only allowed origins can connect      │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│         3. Input Validation              │
│    Sanitize all inputs                   │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│         4. Error Handling                │
│    No sensitive data in responses        │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│         5. Logging & Monitoring          │
│    Track all requests                    │
└──────────────────────────────────────────┘
```

---

## 🔧 Configuration Options

| Variable | Default | Purpose |
|----------|---------|---------|
| `PORT` | 3000 | Server port |
| `NODE_ENV` | development | Environment mode |
| `CACHE_DURATION_MINUTES` | 30 | Cache TTL |
| `RATE_LIMIT_WINDOW_MINUTES` | 5 | Rate limit window |
| `RATE_LIMIT_MAX_REQUESTS` | 10 | Max requests in window |
| `LOG_LEVEL` | info | Logging verbosity |

---

## 📈 Scaling Strategy

### Phase 1: Single Server (Current)
- ✅ Railway/Render (1 instance)
- ✅ In-memory cache
- ✅ Good for 100-1000 users

### Phase 2: Horizontal Scaling
- 🔄 Multiple instances behind load balancer
- 🔄 Redis for shared cache
- 🔄 Good for 1000-10000 users

### Phase 3: Distributed
- 🔮 Microservices architecture
- 🔮 Separate services for each exchange
- 🔮 Message queue (RabbitMQ/Kafka)
- 🔮 Good for 10000+ users

---

**זהו! עכשיו אתה מבין את כל האר כיטקטורה! 🎓**
